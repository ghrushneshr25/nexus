// Nexus provides a lightweight, reflection-based dependency injection container for Go applications. Nexus registers constructors during application initialization and resolves services lazily on first use. Each successfully resolved service is cached as a singleton and reused for later resolutions. Nexus invokes constructors lazily and caches only successful, non-nilresults. Constructor errors and failed resolutions are returned by Get and GetNamed, or cause MustGet and MustGetNamed to panic. Concurrent resolution of the same service constructs it at most once. Nexus also detects direct and indirect circular dependencies during resolution. Validate checks the registered dependency graph without invoking constructors. It detects missing default service declarations, missing concrete values, and direct or indirect circular dependencies.
package nexus
